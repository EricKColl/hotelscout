# HotelScout: no se necesita ninguna clave para el Modo A.
# No pongas nunca secretos en variables VITE_* (se publican en el navegador).
# Contacto que el proxy incluye en el User-Agent hacia Nominatim/Overpass (lo configurara el usuario al desplegar):
PROXY_CONTACT=

